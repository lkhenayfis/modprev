% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-validation.r
\name{evaluate_validation_control}
\alias{evaluate_validation_control}
\title{Evaluate Validation Control Parameter}
\usage{
evaluate_validation_control(validation_control, serie, regdata, validation)
}
\arguments{
\item{validation_control}{Either a list or function. If function, it should
have signature \code{function(serie, regdata)} and return a named list
of validation control parameters}

\item{serie}{Time series object being modeled}

\item{regdata}{Data frame or matrix of regressors}

\item{validation}{Character string specifying validation mode ("cv" or "split")}
}
\value{
Named list of validation control parameters suitable for the
specified validation mode
}
\description{
Evaluates validation_control if it is a function, or passes through if list
}
\details{
This function supports both static list and function-based specification of
validation control parameters. When a function is provided, it is called with
serie and regdata to produce the control list dynamically.
}
\examples{
# Function-based control for adaptive CV
ctrl_fn <- function(serie, regdata) {
    list(nfold = min(10, length(serie) \%/\% 20))
}
evaluate_validation_control(ctrl_fn, ts(1:100), data.frame(x = 1:100), "cv")

# Static list passes through unchanged
evaluate_validation_control(list(nfold = 5), ts(1:100), NULL, "cv")
}
