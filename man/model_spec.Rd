% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-spec.r
\name{model_spec}
\alias{model_spec}
\alias{new_model_spec}
\alias{validate_model_spec}
\title{Model Specification Class}
\usage{
model_spec(
  tipo,
  fit_fn,
  requires_regdata = FALSE,
  deps = character(),
  metadata = list()
)

new_model_spec(tipo, fit_fn, requires_regdata, deps, metadata)

validate_model_spec(spec)
}
\arguments{
\item{tipo}{Character string uniquely identifying the model type. Must be non-empty and
match the class name used in model objects (e.g., \code{"sarima"}, \code{"reg_lin"}).}

\item{fit_fn}{Function for model estimation. Should accept \code{serie} as first argument and
return a \code{modprevU} object.}

\item{requires_regdata}{Logical indicating whether the model requires regression data
(\code{regdata} argument). Defaults to \code{FALSE}.}

\item{deps}{Character vector of required package names. Defaults to \code{character()}.}

\item{metadata}{Optional named list with additional model information (description, author,
version, etc.). Defaults to empty list.}

\item{spec}{A \code{model_spec} object to validate.}
}
\value{
Object of class \code{"model_spec"} with elements: \code{tipo}, \code{fit_fn},
\code{requires_regdata}, \code{deps}, \code{metadata}.
}
\description{
Container for model metadata and function references used by the model registry system.
}
\details{
Stores the estimation function and metadata for a model type. Prediction and update
dispatch happen via plain S3 method dispatch and are not stored here.
}
\examples{
dummy_fit <- function(serie, ...) {
    new_modprevU(list(data = serie), serie, "dummy", list())
}

spec <- model_spec(
    tipo = "dummy",
    fit_fn = dummy_fit,
    deps = "forecast",
    metadata = list(description = "A dummy model")
)

}
\seealso{
Other model_registry: 
\code{\link{get_model}()},
\code{\link{get_registry}()},
\code{\link{is_registered}()},
\code{\link{list_models}()},
\code{\link{register_model}()}
}
\concept{model_registry}
