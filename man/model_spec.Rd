% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-spec.r
\name{model_spec}
\alias{model_spec}
\alias{new_model_spec}
\alias{validate_model_spec}
\title{Model Specification Class}
\usage{
model_spec(
  tipo,
  fit_fn,
  predict_fn,
  update_fn,
  requires_regdata = FALSE,
  deps = character(),
  metadata = list()
)

new_model_spec(
  tipo,
  fit_fn,
  predict_fn,
  update_fn,
  requires_regdata,
  deps,
  metadata
)

validate_model_spec(spec)
}
\arguments{
\item{tipo}{Character string uniquely identifying the model type. Must be non-empty and
match the class name used in model objects (e.g., \code{"sarima"}, \code{"reg_lin"}).}

\item{fit_fn}{Function for model estimation. Should accept \code{serie} as first argument and
return a \code{modprevU} object.}

\item{predict_fn}{Function for generating predictions. Should be an S3 method compatible
with \code{predict()} generic.}

\item{update_fn}{Function for updating models with new data. Should be an S3 method
compatible with \code{update()} generic.}

\item{requires_regdata}{Logical indicating whether the model requires regression data
(\code{regdata} argument). Defaults to \code{FALSE}.}

\item{deps}{Character vector of required package names. Use empty character vector if
no dependencies. Defaults to \code{character()}.}

\item{metadata}{Optional named list containing additional model information such as
description, author, or version. Defaults to empty list.}

\item{spec}{A \code{model_spec} object to validate.}
}
\value{
Object of class \code{"model_spec"} containing model metadata and function references.
}
\description{
Container for model metadata and function references used by the model registry system.
}
\details{
A \code{model_spec} object encapsulates all information needed to register and use a model type
within the modprev framework. It stores references to estimation, prediction, and update
functions, along with metadata about model requirements.
}
\section{Structure}{
The specification includes:
\itemize{
\item \strong{tipo}: Model type identifier (character)
\item \strong{fit_fn}: Function for model estimation
\item \strong{predict_fn}: Function for generating predictions
\item \strong{update_fn}: Function for model updates
\item \strong{requires_regdata}: Whether model needs regression data
\item \strong{deps}: Required package dependencies
\item \strong{metadata}: Additional descriptive information
}
}

\examples{
dummy_fit <- function(serie, ...) {
    new_modprevU(list(data = serie), serie, "dummy", list())
}
dummy_predict <- function(object, n.ahead, ...) {
    ts(rep(0, n.ahead))
}
dummy_update <- function(object, newseries, refit, ...) {
    object
}

spec <- model_spec(
    tipo = "dummy",
    fit_fn = dummy_fit,
    predict_fn = dummy_predict,
    update_fn = dummy_update,
    requires_regdata = FALSE,
    deps = character()
)

spec_with_deps <- model_spec(
    tipo = "sarima",
    fit_fn = dummy_fit,
    predict_fn = dummy_predict,
    update_fn = dummy_update,
    requires_regdata = FALSE,
    deps = "forecast",
    metadata = list(description = "SARIMA model using forecast package")
)

}
\seealso{
Other model_registry: 
\code{\link{get_model}()},
\code{\link{get_registry}()},
\code{\link{is_registered}()},
\code{\link{list_models}()},
\code{\link{register_model}()}
}
\concept{model_registry}
