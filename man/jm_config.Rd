% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classe-jm-config.r
\name{jm_config}
\alias{jm_config}
\title{Create janelamovel Configuration Object}
\usage{
jm_config(
  janela,
  passo = 1L,
  n.ahead = 1L,
  refit.cada = NA,
  verbose = 0L,
  full.output = FALSE
)
}
\arguments{
\item{janela}{Scalar or two-element vector specifying window size. If scalar,
rolling window of that size. If vector, expanding window starting at
\code{janela[1]} with initial width \code{janela[2]}.}

\item{passo}{Integer step size between windows. Default 1L.}

\item{n.ahead}{Integer number of steps ahead to forecast. Default 1L.}

\item{refit.cada}{Interval for model refitting. If NA (default), model is
fit once and updated. If integer, model is refit every \code{refit.cada} windows.}

\item{verbose}{Integer verbosity level: 0 = silent, 1 = refit events,
2 = all windows. Default 0.}

\item{full.output}{Logical indicating whether to return full output (predictions,
models, regdata) or just predictions. Default FALSE.}
}
\value{
S3 object of class \code{jm_config} containing validated configuration
}
\description{
Build a configuration object for janelamovel execution with validated parameters.
}
\examples{
# Rolling window of 60, predict 1 step ahead
config <- jm_config(janela = 60)

# Expanding window, predict 12 steps
config <- jm_config(janela = c(1, 50), n.ahead = 12)

# With refit and verbose output
config <- jm_config(janela = 60, passo = 6, refit.cada = 6, verbose = 2)

}
