% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-validation.r
\name{validate_control_boost_cv}
\alias{validate_control_boost_cv}
\title{Validate BOOST CV Mode Control Parameters}
\usage{
validate_control_boost_cv(control_list)
}
\arguments{
\item{control_list}{Named list of validation control parameters}
}
\value{
The input control_list (invisibly) if valid, otherwise raises error
}
\description{
Checks that control list contains valid parameters for BOOST cross-validation
}
\details{
This is a lightweight validator that checks basic structure only. BOOST CV
uses \code{match_fun_args()} to extract parameters for \code{mboost::cv()},
\code{mboost::cvrisk()}, and \code{parallel::mclapply()}, so comprehensive validation
is not feasible. We validate only critical parameters to catch confusing
errors early.
}
\examples{
# Minimal valid control
validate_control_boost_cv(list())

# With CV parameters
validate_control_boost_cv(list(type = "kfold", B = 10))
}
